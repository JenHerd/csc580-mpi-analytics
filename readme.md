make sure that the mpi_analytics.exe file you just compiled is copied to the exact same folder path on your laptop and the other two worker laptops. The MPI service needs to find the file locally on all machines to execute it.

**C:\MPI_Project**
